# Скопируйте в .env и подставьте свои значения. Файл .env не коммитится.
# Для Docker: в DATABASE_URL используйте хост db. Для npm run dev — localhost.

# PostgreSQL (в URL пароль: $ → %24, % → %25)
DATABASE_URL=postgresql://materiamedica:YOUR_PASSWORD@db:5432/materiamedica
POSTGRES_PASSWORD=your_postgres_password

# Node.js
SESSION_SECRET=your_session_secret_min_32_chars

# Yandex Cloud S3
YC_BUCKET=your_bucket
YC_REGION=ru-central1
YC_ACCESS_KEY_ID=
YC_SECRET_ACCESS_KEY=

# Robokassa
ROBOKASSA_LOGIN=
ROBOKASSA_PASSWORD1=
ROBOKASSA_PASSWORD2=

# Для docker-compose.dev: COOKIE_SECURE=false задаётся в compose. Для прода — .env.prod с COOKIE_SECURE=true
# APP_URL=https://your-domain.ru
